for (let index = 1; index < 11; index++) {
    const element = index;
    console.log(`this is ${index} iteration`) 
}
// we can use here const because there is created a new block for every iterations happening
// so we can use the const here

// braek --> stops the execution of loop 
// continue --> skips the one iteration (only code which is after the countinue)
// means continue jumps to next iteration without performing reamining statements of block of loop